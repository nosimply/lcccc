//在学习node的时候又碰到了js的回调函数，这在异步调用时是很常见的，自己又有一些新的体会:
node(function(data){
alert(data);
})
node:function(fun){//这里的fun是一个函数名字，其实是一个指针。
fun('hello');//这里的fun已经被调用了，会去调用的是这个指针指向的函数。
}
如果还是不懂可以看看c语言中的指针，那是很适合的。
